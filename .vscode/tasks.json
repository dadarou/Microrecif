{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make --directory=${workspaceFolder}/Code",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}